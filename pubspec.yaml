name: after_the_credits
description: "A clean and personalized interface for aftercredits.com and Letterboxd RSS."
publish_to: 'none'
version: 1.1.1+3

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.3+1
  path: ^1.9.0
  http: ^1.2.1
  html: ^0.15.4
  xml: ^6.5.0
  intl: ^0.19.0
  url_launcher: ^6.2.5
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  webview_flutter: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
